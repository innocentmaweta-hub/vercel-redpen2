import React, { forwardRef } from 'react';

export const GradeToolbar = forwardRef(function GradeToolbar(props: any, ref: any) {
  return (
    <div ref={ref} className="grade-toolbar">
      {/* Minimal toolbar placeholder */}
    </div>
  );
});

export default GradeToolbar;
