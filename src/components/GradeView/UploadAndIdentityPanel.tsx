import React from 'react';

export function UploadAndIdentityPanel(props: any) {
  const { grading } = props;
  return (
    <div className="upload-identity-panel">
      {/* Minimal upload + student identity placeholder */}
      <div>Upload / Identity</div>
    </div>
  );
}

export default UploadAndIdentityPanel;
